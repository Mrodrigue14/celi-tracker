# AGP + Compose (KSP, dexing) exceed the Gradle daemon's default heap.
org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=1024m

# Required by the AndroidX dependencies (Compose, Room, ...).
android.useAndroidX=true
