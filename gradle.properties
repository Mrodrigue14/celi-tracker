# AGP + Compose (KSP, dexing) depassent le tas par defaut du daemon Gradle.
org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=1024m

# Requis par les dependances AndroidX (Compose, Room, ...).
android.useAndroidX=true
